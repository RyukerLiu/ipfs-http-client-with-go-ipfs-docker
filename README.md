# IPFS HTTP Client Python Package

Reference: https://github.com/ipfs/py-ipfs-http-client

## Start go-ipfs

``` bash
cd go-ipfs-docker
docker-compose up -d
```

## Run IPFS HTTP Client Sample

``` bash
pipenv install
pipenv shell
python ipfs_http_client_sample.py
```
